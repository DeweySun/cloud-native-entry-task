apiVersion: v1
kind: Service
metadata:
  name: dbcp-entry-service
  namespace: default
  labels:
    app.kubernetes.io/name: dbcp-entry-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: dbcp-entry-service
  ports:
    - name: http
      port: 8080
      targetPort: http

